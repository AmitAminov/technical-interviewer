[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: tests that load the sentence-transformers embedding model (session-scoped fixture index)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
